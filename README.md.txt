# Waste Sorter Bot

How to deploy:
1. Push this repo to GitHub.
2. Connect repo on Railway and set environment variable BOT_TOKEN (your Telegram token).
3. Railway will install requirements and run `worker: python WasteSorterAI.py`.

DO NOT put your token inside the code or push any `.env`/`config.py`.
